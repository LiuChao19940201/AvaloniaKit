<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" android:installLocation="auto">
	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.CAMERA" />
	<uses-permission android:name="android.permission.VIBRATE" />

	<!-- Android 11+ 需要声明 queries 才能通过 ResolveActivity 查找相机应用 -->
	<queries>
		<intent>
			<action android:name="android.media.action.IMAGE_CAPTURE" />
		</intent>
	</queries>

	<!-- 网易云 302 跳转后的 CDN 地址为明文 http（mxxx.music.126.net），
	     Android 9+ 默认禁止明文流量，必须显式放行，否则 MediaPlayer/HttpClient 静默失败 -->
	<application android:label="AvaloniaKit" android:icon="@drawable/Icon" android:usesCleartextTraffic="true" />
</manifest>